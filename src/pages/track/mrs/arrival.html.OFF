<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Mrs · Arrival</title>
    <meta name="robots" content="noindex" />
    <style>
      body{
        margin:0;
        min-height:100vh;
        color: rgba(235,240,255,0.86);
        background:
          radial-gradient(1200px 700px at 18% 10%, rgba(160,130,255,0.18), transparent 60%),
          radial-gradient(900px 600px at 82% 18%, rgba(120,220,255,0.10), transparent 60%),
          radial-gradient(900px 620px at 55% 85%, rgba(255,190,90,0.07), transparent 62%),
          linear-gradient(180deg, #070812 0%, #0a0d18 48%, #070812 100%);
      }
      main{
        max-width: 54rem;
        margin: 0 auto;
        padding: 3.25rem 1.25rem 3rem;
      }
      .card{
        padding: 1.5rem 1.75rem;
        background: rgba(12,14,26,0.50);
        border: 1px solid rgba(255,255,255,0.12);
        border-radius: 14px;
        backdrop-filter: blur(6px);
        box-shadow: 0 10px 26px rgba(0,0,0,0.36);
      }
      h1{margin:0 0 .75rem 0;font-size:2rem;letter-spacing:.01em;}
      p{margin:0 0 1rem 0;color: rgba(235,240,255,0.68);line-height:1.55;}
      a{color: rgba(170,160,255,0.92);text-decoration:none;font-weight:600;}
      a:hover{text-decoration:underline;}
      nav{margin-top:1.25rem; display:flex; gap:1rem; flex-wrap:wrap;}
      .faint{opacity:.55;font-size:.95rem;}
    </style>
  </head>
  <body>
    <main>
      <section class="card">
        <h1>Mrs · Arrival</h1>
        <p class="faint">This page is live. No auto-redirects.</p>

        <nav>
          <a href="/track/mrs/first-room/">first room</a>
          <a href="/track/mrs/room-v2/">room v2</a>
          <a href="/courtyard/">courtyard</a>
          <a href="/edge/">exit</a>
        </nav>
      </section>
    </main>
  </body>
</html>
